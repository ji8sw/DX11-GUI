#pragma once
#include "MenuRoot.hpp"

/*
	Actions are simply buttons, they perform an action when clicked.
	Command already includes on_click functionality, so Action does not need to add anything new.
*/
class Action : public Command
{

};